"use strict";

/**
 * department-statistic router
 */

const { createCoreRouter } = require("@strapi/strapi").factories;

module.exports = createCoreRouter(
  "api::department-statistic.department-statistic"
);
